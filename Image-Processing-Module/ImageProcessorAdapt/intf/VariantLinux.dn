

component provides intf.ImgProcessor requires io.Output out, io.Input stdin, io.File, os.Run run {
	void ImgProcessor:process() {

		out.println("VariantLinux")

		/* char osCommand[] = new char[]("mkdir /home testedana1")
		RunStatus rS1 = run.execute(osCommand)

		char osCommand[] = new char[]("mkdir /home testedana2")
		RunStatus rS2 = run.execute(osCommand)

		char osCommand[] = new char[]("mkdir /home testedana3")
		RunStatus rS3 = run.execute(osCommand) */

	}
}
